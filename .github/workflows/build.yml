name: Build binaries

on:
  workflow_dispatch:
    inputs:
      debug_console:
        description: "Windows-Build mit sichtbarer Konsole (zum Debuggen von Abstuerzen), statt fuer den Release"
        type: boolean
        default: false
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      # --- Native MediaInfo-Bibliothek (Laufzeit-Abhaengigkeit von pymediainfo) ---
      - name: Install MediaInfo (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libmediainfo-dev patchelf desktop-file-utils
      - name: Install MediaInfo (macOS)
        if: runner.os == 'macOS'
        env:
          HOMEBREW_NO_REQUIRE_TAP_TRUST: "1"
        run: brew install media-info libmediainfo
      - name: Install MediaInfo (Windows)
        if: runner.os == 'Windows'
        run: choco install mediainfo --no-progress

      - name: Install Python dependencies
        run: |
          python -m pip install -r requirements-dev.txt
          python -m pip install -e .

      # --- Nuitka-Build ---
      # Hinweis: pymediainfo laedt libmediainfo per ctypes. Auf Linux/macOS
      # erkennt Nuitkas dll-files-Plugin die Lib automatisch und bündelt sie,
      # solange sie beim Build installiert ist. Nur unter Windows muss die DLL
      # manuell als Data-File angegeben werden (kein automatisches ctypes-Scan
      # fuer WinDLL-Aufrufe).
      - name: Build with Nuitka (Linux)
        if: runner.os == 'Linux'
        run: |
          python -m nuitka \
            --onefile \
            --remove-output \
            --enable-plugin=pyside6 \
            --include-package=pymediainfo \
            --include-data-dir=translations=translations \
            --output-filename=CodecLister_linux \
            --output-dir=nuitka-out \
            --assume-yes-for-downloads \
            src/codeclister/__main__.py
      - name: Build with Nuitka (macOS)
        if: runner.os == 'macOS'
        run: |
          python -m nuitka \
            --onefile \
            --remove-output \
            --enable-plugin=pyside6 \
            --include-package=pymediainfo \
            --include-data-dir=translations=translations \
            --output-filename=CodecLister_mac \
            --output-dir=nuitka-out \
            --assume-yes-for-downloads \
            src/codeclister/__main__.py
      - name: Build with Nuitka (Windows)
        if: runner.os == 'Windows'
        run: |
          $dll = "C:\Program Files\MediaInfo\MediaInfo.dll"
          # Konsole bleibt sichtbar, wenn der Lauf ueber workflow_dispatch mit debug_console=true gestartet wurde.
          $consoleMode = "${{ github.event.inputs.debug_console }}" -eq "true" ? "force" : "disable"
          python -m nuitka `
            --onefile `
            --remove-output `
            --enable-plugin=pyside6 `
            --include-package=pymediainfo `
            --include-data-dir=translations=translations `
            --include-data-files="$dll=MediaInfo.dll" `
            --windows-console-mode=$consoleMode `
            --output-filename=CodecLister.exe `
            --output-dir=nuitka-out `
            --assume-yes-for-downloads `
            src/codeclister/__main__.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: CodecLister-${{ matrix.os }}
          path: nuitka-out/*
          if-no-files-found: error

  release:
    name: Publish release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      # Verhindert "Not Found"-Fehler von action-gh-release, wenn fuer den Tag
      # bereits ein (z. B. manuell geloeschtes/inkonsistentes) Release existiert.
      - name: Delete existing release for this tag (if any)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release delete "${{ github.ref_name }}" --yes || true
      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
