{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CodecLister (GUI)",
            "type": "debugpy",
            "request": "launch",
            "module": "codeclister",
            "justMyCode": true,
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}
